"""\npricin_sheet_classifier_v2.py\nUniversal sheet classifier with auto category detection.\nWorks with NO template — signal-based detection across all 20 procurement categories.\n"""\n\nfrom __future__ import annotations\nimport logging\nfrom typing import Dict, List, Optional, Tuple\n\nfrom .procurement_taxonomy import (\n    TAXONOMY, GENERIC, get_category, resolve_column_alias,\n    ALL_CODES,\n)\n\nlogger = logging.getLogger(__name__)\n\nROLE_PRICING        = \"PRICING\"\nROLE_TECHNICAL      = \"TECHNICAL\"\nROLE_COMMERCIAL     = \"COMMERCIAL\"\nROLE_COVER          = \"COVER\"\nROLE_SUMMARY        = \"SUMMARY\"\nROLE_QUALIFICATIONS = \"QUALIFICATIONS\"\nROLE_IGNORE         = \"IGNORE\"\n\n_COVER_SIGNALS      = {\"cover\", \"intro\", \"introduction\", \"overview\", \"instructions\", \"contents\", \"toc\"}\n_SUMMARY_SIGNALS    = {\"summary\", \"total\", \"overall\", \"consolidated\", \"comparison\", \"scorecard\"}\n_QUAL_SIGNALS       = {\"qualification\", \"compliance\", \"certifications\", \"accreditations\", \"quality\", \"hse\"}\n_PRICING_SIGNALS    = {\"price\", \"pricing\", \"rate\", \"rates\", \"cost\", \"quote\", \"quotation\", \"bom\",\n                       \"bill of materials\", \"boq\", \"bill of quantities\", \"commercial\"}\n_TECHNICAL_SIGNALS  = {\"technical\", \"specification\", \"specifications\", \"tech\", \"product\", \"scope\",\n                       \"deliverable\", \"service description\"}\n\nUNIVERSAL_CANONICAL = {\n    \"item\": \"item_id\", \"item id\": \"item_id\", \"item no\": \"item_id\", \"sr no\": \"item_id\",\n    \"sl no\": \"item_id\", \"s.no\": \"item_id\", \"#\": \"item_id\",\n    \"description\": \"description\", \"item description\": \"description\",\n    \"product\": \"description\", \"service\": \"description\", \"material\": \"description\",\n    \"qty\": \"quantity\", \"quantity\": \"quantity\", \"volume\": \"quantity\",\n    \"uom\": \"uom\", \"unit\": \"uom\", \"unit of measure\": \"uom\", \"u/m\": \"uom\",\n    \"unit price\": \"unit_price\", \"unit cost\": \"unit_price\", \"rate\": \"unit_price\",\n    \"price\": \"unit_price\", \"cost\": \"unit_price\", \"amount\": \"unit_price\",\n    \"total\": \"total_price\", \"total price\": \"total_price\", \"total cost\": \"total_price\",\n    \"extended price\": \"total_price\", \"net amount\": \"total_price\",\n    \"currency\": \"currency\", \"curr\": \"currency\",\n    \"lead time\": \"lead_time\", \"delivery\": \"lead_time\", \"lead time (weeks)\": \"lead_time\",\n    \"payment terms\": \"payment_terms\", \"terms\": \"payment_terms\",\n    \"validity\": \"validity\", \"valid till\": \"validity\", \"quote validity\": \"validity\",\n    \"remarks\": \"notes\", \"notes\": \"notes\", \"comments\": \"notes\",\n}\n\n\ndef detect_category(sheet_data: Dict[str, List[List]], scan_rows: int = 60) -> str:\n    scores: Dict[str, float] = {code: 0.0 for code in ALL_CODES if code != GENERIC}\n    for sheet_name, rows in sheet_data.items():\n        cell_text = _flatten_cells(rows[:scan_rows])\n        for code, cat in TAXONOMY.items():\n            if code == GENERIC:\n                continue\n            for signal in cat.technical_signals:\n                if signal.lower() in cell_text:\n                    scores[code] = scores.get(code, 0.0) + 1.0\n            for uom in cat.uom_factors:\n                if uom.lower() in cell_text:\n                    scores[code] = scores.get(code, 0.0) + 0.5\n    if not scores:\n        return GENERIC\n    winner = max(scores, key=lambda k: scores[k])\n    top_score = scores[winner]\n    if top_score < 2.0:\n        logger.warning(f\"[classifier_v2] No category scored >= 2 (best: {winner}={top_score:.1f}). Defaulting to GENERIC.\")\n        return GENERIC\n    logger.info(f\"[classifier_v2] Detected category: {winner} (score={top_score:.1f})\")\n    return winner\n\n\ndef _flatten_cells(rows: List[List]) -> str:\n    parts = []\n    for row in rows:\n        for cell in row:\n            if cell is not None:\n                parts.append(str(cell).lower().strip())\n    return \" \".join(parts)\n\n\ndef map_header_to_canonical(raw_header: str, category_code: str = GENERIC, llm_invoke_fn=None) -> Optional[str]:\n    key = raw_header.strip().lower()\n    if key in UNIVERSAL_CANONICAL:\n        return UNIVERSAL_CANONICAL[key]\n    alias = resolve_column_alias(raw_header, category_code)\n    if alias:\n        return alias\n    for k, v in UNIVERSAL_CANONICAL.items():\n        if k in key or key in k:\n            return v\n    return None\n\n\ndef map_headers(headers: List[str], category_code: str = GENERIC, llm_invoke_fn=None) -> Tuple[Dict[int, str], List[str]]:\n    mapped: Dict[int, str] = {}\n    unmapped: List[str] = []\n    for i, h in enumerate(headers):\n        if not h or str(h).strip() == \"\":\n            continue\n        canonical = map_header_to_canonical(h, category_code, llm_invoke_fn)\n        if canonical:\n            mapped[i] = canonical\n        else:\n            unmapped.append(h)\n    if len(mapped) < 2 and llm_invoke_fn and unmapped:\n        logger.info(f\"[classifier_v2] Only {len(mapped)} headers mapped — invoking LLM fallback.\")\n        llm_mapped = _llm_map_headers(unmapped, category_code, llm_invoke_fn)\n        for i, h in enumerate(headers):\n            if h in llm_mapped:\n                mapped[i] = llm_mapped[h]\n                unmapped = [u for u in unmapped if u != h]\n    return mapped, unmapped\n\n\ndef _llm_map_headers(headers: List[str], category_code: str, llm_invoke_fn) -> Dict[str, str]:\n    cat = get_category(category_code)\n    all_canonicals = list(UNIVERSAL_CANONICAL.values()) + cat.key_columns\n    prompt = (\n        f\"You are a procurement data analyst. Map these spreadsheet headers from a '{cat.label}' RFP \"\n        f\"to canonical fields from: {all_canonicals}. Return JSON only: {{header: canonical_name}}.\\n\"\n        f\"Headers: {headers}\"\n    )\n    try:\n        result = llm_invoke_fn(prompt)\n        import json\n        return json.loads(result) if isinstance(result, str) else result\n    except Exception as e:\n        logger.warning(f\"[classifier_v2] LLM header mapping failed: {e}\")\n        return {}\n\n\ndef classify_sheet_role(sheet_name: str, rows: List[List]) -> str:\n    name_lower = sheet_name.strip().lower()\n    for signal in _COVER_SIGNALS:\n        if signal in name_lower: return ROLE_COVER\n    for signal in _SUMMARY_SIGNALS:\n        if signal in name_lower: return ROLE_SUMMARY\n    for signal in _QUAL_SIGNALS:\n        if signal in name_lower: return ROLE_QUALIFICATIONS\n    for signal in _PRICING_SIGNALS:\n        if signal in name_lower: return ROLE_PRICING\n    for signal in _TECHNICAL_SIGNALS:\n        if signal in name_lower: return ROLE_TECHNICAL\n    cell_text = _flatten_cells(rows[:5])\n    numeric_count = sum(1 for r in rows[:20] for c in r if c is not None and _is_numeric(str(c)))\n    if any(s in cell_text for s in [\"unit price\", \"unit cost\", \"rate\", \"total\", \"quote\"]):\n        return ROLE_PRICING\n    if any(s in cell_text for s in [\"specification\", \"technical\", \"scope\"]):\n        return ROLE_TECHNICAL\n    if numeric_count > 10:\n        return ROLE_PRICING\n    return ROLE_IGNORE\n\n\ndef _is_numeric(val: str) -> bool:\n    try:\n        float(val.replace(\",\", \"\").replace(\"$\", \"\").strip())\n        return True\n    except (ValueError, AttributeError):\n        return False\n\n\ndef classify_workbook_sheets(sheet_data: Dict[str, List[List]], category: str = GENERIC, llm_invoke_fn=None) -> Dict[str, dict]:\n    result = {}\n    for sheet_name, rows in sheet_data.items():\n        if not rows:\n            continue\n        role = classify_sheet_role(sheet_name, rows)\n        header_row_idx = 0\n        for i, row in enumerate(rows[:10]):\n            non_empty = [c for c in row if c is not None and str(c).strip()]\n            if len(non_empty) >= 2:\n                header_row_idx = i\n                break\n        headers = [str(c).strip() if c is not None else \"\" for c in rows[header_row_idx]]\n        header_map, unmapped = map_headers(headers, category, llm_invoke_fn)\n        result[sheet_name] = {\n            \"role\": role,\n            \"header_row_idx\": header_row_idx,\n            \"header_map\": header_map,\n            \"unmapped_headers\": unmapped,\n            \"headers_raw\": headers,\n            \"rows\": rows[header_row_idx + 1:],\n        }\n        logger.info(f\"[classifier_v2] Sheet='{sheet_name}' | Role={role} | Mapped={len(header_map)} | Unmapped={len(unmapped)}\")\n    return result\n